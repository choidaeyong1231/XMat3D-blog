# 1편: 2D 영상으로 3D 검사기를 만든 이유

> *"2D 엑스레이 이미지만 봐서는 솔더볼이나 칩이 위로 얼마나 솟았는지(단차) 알 수가 없다..."*

## 1. 현장에서 마주한 고민
반도체, 전자 패키징, SMT 공정 현장에서는 부품의 품질 검사를 위해 **X-Ray 검사 장비**나 **고해상도 광학 현미경**을 사용합니다. 

카메라를 통해 획득되는 이미지는 주로 **16비트 그레이스케일(Grayscale)** 포맷입니다. 픽셀 밝기(Intensity)는 물질의 밀도와 두께 정보를 담고 있지만, 화면에 표시되는 것은 **평면적인 2D 흑백 사진**일 뿐입니다.

여기서 현장 엔지니어들이 겪는 치명적인 한계가 발생합니다:

* **단차(높이 차이)의 직관적 파악 불가**:
  두 지점의 밝기가 비슷해 보여도 실제 단차가 수 마이크로미터($\mu m$) 이상 차이 나는지 2D 영상만 보고는 작업자가 판단하기 매우 어렵습니다.
* **비싼 3D CT 장비 도입의 부담**:
  완전한 3D 복원(CT) 장비는 1대당 수억 원을 호가하며, 촬영 및 재구성(Reconstruction)에 수십 초~수분이 걸려 전수 검사 라인에 투입하기 어렵습니다.
* **상용 3D 툴의 폐쇄성과 라이선스 비용**:
  외산 전문 분석 소프트웨어는 라이선스 동글키 비용만 수천만 원에 달하며, 현장 작업자가 원하는 특수한 계산(예: ROI 간 실시간 뺄셈 단차 계산, 특정 임계값 노이즈 보간)을 추가하기 어렵습니다.

---

## 2. 해결 목표: "가볍고 빠른 2D 기반 가상 3D 분석기"
이러한 문제를 해결하기 위해 **가볍고 빠른 독립형 C# 분석 툴**을 직접 만들기로 결정했습니다.

```
[2D 검사 영상 (16-bit TIFF)]
           │
           ▼ (마우스 드래그로 관심 영역 ROI 선택)
[OpenCvSharp 고속 전처리 & 정규화]
           │
           ▼ (Z축 깊이 스케일링 & 3D 정점 산출)
[OpenTK / OpenGL 정점 버퍼(VBO) 메쉬 생성]
           │
           ▼ (실시간 인터랙티브 조작)
[3D 회전/줌, 단차 계측, 평면 피팅(Plane Fitting), TIF 내보내기]
```

핵심 목표는 다음과 같았습니다:
1. **즉각적인 반응 속도**: 2D 화면에서 사각형 ROI를 그리는 순간, 0.5초 이내에 3D 표면 메쉬가 생성되어야 한다.
2. **독립 모듈화**: 다른 산업용 장비 제어 프로그램에 컴포넌트(DLL)로 쉽게 얹어서 납품할 수 있어야 한다.
3. **작업자 중심의 실전 워크플로**: 복잡한 3D 모델링 기능 대신, **단차 사칙연산, 평면 기울기 보정, 노이즈 필터링** 등 현장 엔지니어가 매일 쓰는 기능에 집중한다.

---

## 3. 전체 시스템 아키텍처
소프트웨어는 모듈 간 결합도를 낮추기 위해 기능별 라이브러리로 분리하여 개발되었습니다:

* **`XScaleViewCtrl` (2D 이미지 뷰어 모듈)**:
  - 수천~수만 픽셀 해상도의 대용량 TIFF 이미지를 버벅임 없이 줌/팬(Pan)할 수 있는 가상 버퍼 시스템.
  - 마우스 드래그로 사각형, 라인, 원형 ROI를 자유롭게 지정하는 오버레이 엔진.
* **`XMat3DVBPlot` (3D 가시화 엔진)**:
  - OpenTK 기반으로 GPU VBO(Vertex Buffer Object)를 생성하여 10만 개 이상의 정점을 60fps로 실시간 회전/음영(Shading) 처리.
  - 컬러맵(Rainbow, Hot, Jet 등)을 통한 높이별 색상 시각화.
* **`XMat3DAnalyzer` (메인 애플리케이션)**:
  - Krypton Toolkit 도킹 창 프레임워크를 기반으로 2D 뷰, 3D 뷰, 분석 파라미터 그리드, 작업 이력을 한 화면에 통합.

---

다음 2편에서는 **C# WinForms 환경에서 OpenTK를 활용해 어떻게 대용량 이미지 데이터를 60fps 3D 메쉬로 고속 렌더링했는지**, 실제 그래픽스 파이프라인 구현 노하우를 다룹니다.
